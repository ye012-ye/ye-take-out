<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into orders(number, status, user_id, address_book_id, order_time, checkout_time, pay_method, pay_status,
                           amount, remark, phone, address, user_name, consignee, cancel_reason, rejection_reason, cancel_time, estimated_delivery_time,
                           delivery_status, delivery_time, pack_amount, tableware_number, tableware_status)
        values (#{number}, #{status}, #{userId}, #{addressBookId}, #{orderTime}, #{checkoutTime}, #{payMethod}, #{payStatus},
                #{amount}, #{remark}, #{phone}, #{address}, #{userName}, #{consignee}, #{cancelReason}, #{rejectionReason}, #{cancelTime}, #{estimatedDeliveryTime},
                #{deliveryStatus}, #{deliveryTime}, #{packAmount}, #{tablewareNumber}, #{tablewareStatus}
               )


    </insert>
    <update id="updateById">
        update orders
        <set>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="cancelReason != null">
                cancel_reason = #{cancelReason},
            </if>
            <if test="rejectionReason != null">
                rejection_reason = #{rejectionReason},
            </if>
            <if test="deliveryTime != null">
                delivery_time = #{deliveryTime},
            </if>
            <if test="cancelTime != null">
                cancel_time = #{cancelTime}
            </if>
        </set>
        where id = #{id}
    </update>
    <select id="page" resultType="com.sky.entity.Orders">
        select * from orders 
        <where>
            <if test="beginTime != null">
                AND order_time >= #{beginTime}  <!-- 数据库字段名：begin_time -->
            </if>
            <!-- 结束时间不为空：小于等于传入的结束时间 -->
            <if test="endTime != null">
                AND  #{endTime} >= order_time    <!-- 数据库字段名：end_time -->
            </if>
            <if test="status != null">and status = #{status}</if>
            <if test="phone != null">and phone like concat('%',#{phone},'%')</if>
            <if test="number != null">and number like concat('%',#{number},'%')</if>
        </where>
    </select>
    <select id="getById" resultType="com.sky.entity.Orders">
        select * from orders
        where id = #{id}
    </select>
    <select id="getStatusCount" resultType="com.sky.vo.OrderStatisticsVO">
        SELECT
            COUNT(CASE WHEN status = 3 THEN 1 END) AS confirmed,  -- 待接单数量
            COUNT(CASE WHEN status = 4 THEN 1 END) AS deliveryInProgress,       -- 待派送数量
            COUNT(CASE WHEN status = 2 THEN 1 END) AS toBeConfirmed -- 派送中数量
        FROM orders
    </select>
    <select id="getOrdersPage" resultType="com.sky.entity.Orders">
        select * from orders
        <where>
            <if test="status != null">
                status = #{status}
            </if>
        </where> order by order_time desc
    </select>
    <select id="sumByMap" resultType="java.lang.Double">
        select sum(amount) from orders
        <where>
            <if test="begin != null">
                and delivery_time &gt; #{begin}
            </if>
            <if test="end != null">
                and delivery_time &lt; #{end}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>


    </select>
</mapper>
