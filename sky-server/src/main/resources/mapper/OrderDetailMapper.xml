<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">


    <insert id="insertBach">
        insert into order_detail(name, image, order_id, dish_id, setmeal_id, dish_flavor, number, amount)
        values
            <foreach collection="orderDetailList" item="o" separator=",">
                (#{o.name}, #{o.image}, #{o.orderId}, #{o.dishId}, #{o.setmealId}, #{o.dishFlavor}, #{o.number}, #{o.amount})
            </foreach>

    </insert>
    <select id="getByOrdersId" resultType="com.sky.entity.OrderDetail">
        select * from order_detail
        where order_id = #{id}
    </select>
</mapper>
